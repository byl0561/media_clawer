# Keep host build artifacts out of the Docker build context.
# (Stage 1 runs `npm install`; a host-built node_modules would corrupt
#  esbuild/rollup native binaries inside the linux image.)
.git
.idea
.DS_Store

frontend/node_modules
frontend/dist

backend/venv
**/__pycache__
*.py[cod]
